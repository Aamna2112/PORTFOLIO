# Aamna Haveri Portfolio

A responsive, dependency-free portfolio site personalized from Aamna's résumé. Open `index.html` in a modern browser or serve the folder with any static web server.

The hero uses the supplied base and reveal portraits with a pointer/touch-controlled CSS radial mask.
